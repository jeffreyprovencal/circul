services:
  - type: web
    runtime: node
    name: app
    buildCommand: npm install && cp -r shared public/shared && npm run migrate
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
